# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Локальные соединения (внутри Docker)
local   all             all                                     trust
host    all             all             127.0.0.1/32            md5
host    all             all             ::1/128                 md5

# Docker сеть
host    all             all             172.16.0.0/12           md5
host    all             all             10.0.0.0/8              md5

# Внешние соединения - вариант 1: доступ со всех IP
host    all             all             0.0.0.0/0               md5
host    all             all             ::/0                    md5

# Внешние соединения - вариант 2: доступ только с определенных IP
# host    all             all             192.168.1.0/24          md5
# host    all             all             203.0.113.0/24          md5

# Внешние соединения - вариант 3: доступ только для определенного пользователя
# host    mydatabase      appuser         0.0.0.0/0               md5
# host    mydatabase      readonly        0.0.0.0/0               md5

# Внешние соединения - вариант 4: доступ с паролем и ограничением
# host    all             all             0.0.0.0/0               scram-sha-256